services:
  redis:
    image: redis/redis-stack:latest
    environment:
      REDIS_ARGS: "--save 60 1000 --appendonly yes"
    ports:
      - 6379:6379
      - 8001:8001
    volumes:
      - ./tmp/redis:/data
    restart: always
